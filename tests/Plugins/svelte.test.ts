import { describe, it } from "vitest";

// Original Knip test: plugins/svelte.test.ts
// Skipped because OptiPrune does not expose the imported module ../../src/compilers/svelte.js.
describe("plugins/svelte.test.ts", () => {
  it.todo("OptiPrune compatibility for missing module ../../src/compilers/svelte.js");
});
