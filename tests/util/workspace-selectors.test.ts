import { describe, it } from "vitest";

// Original Knip test: util/workspace-selectors.test.ts
// Skipped because OptiPrune does not expose the imported module ../../src/types/package-json.js.
describe("util/workspace-selectors.test.ts", () => {
  it.todo("OptiPrune compatibility for missing module ../../src/types/package-json.js");
});
