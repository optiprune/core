import { describe, it } from "vitest";

// Original Knip test: util/resolve.test.ts
// Skipped because OptiPrune does not expose the imported module ../../src/util/resolve.js.
describe("util/resolve.test.ts", () => {
  it.todo("OptiPrune compatibility for missing module ../../src/util/resolve.js");
});
