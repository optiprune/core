import { describe, it } from "vitest";

// Original Knip test: util/workspace.test.ts
// Skipped because OptiPrune does not expose the imported module ../../src/util/workspace.js.
describe("util/workspace.test.ts", () => {
  it.todo("OptiPrune compatibility for missing module ../../src/util/workspace.js");
});
