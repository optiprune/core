import { describe, it } from "vitest";

// Original Knip test: util/table.test.ts
// Skipped because OptiPrune does not expose the imported module ../../src/util/table.js.
describe("util/table.test.ts", () => {
  it.todo("OptiPrune compatibility for missing module ../../src/util/table.js");
});
