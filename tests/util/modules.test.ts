import { describe, it } from "vitest";

// Original Knip test: util/modules.test.ts
// Skipped because OptiPrune does not expose the imported module ../../src/util/modules.js.
describe("util/modules.test.ts", () => {
  it.todo("OptiPrune compatibility for missing module ../../src/util/modules.js");
});
