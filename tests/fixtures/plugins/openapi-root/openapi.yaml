openapi: 3.0.0
info:
  title: Root API
  version: 1.0.0
paths: {}
components:
  schemas:
    Used:
      type: object
      properties:
        self:
          $ref: '#/components/schemas/Used'
    Unused:
      type: object
