import { Controller, Get, Module } from "@nestjs/common"; import { NestFactory } from "@nestjs/core"; @Controller() class AppController { @Get() health() { return { ok: true }; } } @Module({ controllers: [AppController] }) class AppModule {} export async function bootstrap() { const app = await NestFactory.create(AppModule); return app; }
