# @optiprune/language-server

Language Server Protocol integration for OptiPrune.

Install the package and configure an LSP client to start `optiprune-language-server` over standard input and output. The server analyzes the workspace identified by the LSP `rootUri` or workspace folders and publishes OptiPrune findings as diagnostics.
