/// <reference types="vite/client" />

/** @type {import('comment-runtime').Config} */
export const config = {} as unknown as import("comment-runtime").Config;

/** @type {import('@types/comment-runtime').Config} */
export const typed = config;
