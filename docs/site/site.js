const app = document.querySelector('#app');
const esc = (s='') => String(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const safeHtml = s => { const t=document.createElement('template'); t.innerHTML=s; t.content.querySelectorAll('script,iframe,object,embed,style,form').forEach(n=>n.remove()); t.content.querySelectorAll('*').forEach(n=>[...n.attributes].forEach(a=>{if(/^on/i.test(a.name)||((a.name==='href'||a.name==='src')&&/^javascript:/i.test(a.value)))n.removeAttribute(a.name)})); return t.innerHTML; };
const md = (s='') => { if (/^\s*</.test(s)) return safeHtml(s); return s.split(/\n\n+/).map(block => { if (/^```/.test(block)) return `<pre>${esc(block.replace(/^```\w*\n?|```$/g,''))}</pre>`; if (/^#{1,3}\s/.test(block)) { const m=block.match(/^(#{1,3})\s+(.+)/); return `<h${m[1].length}>${inline(m[2])}</h${m[1].length}>`; } if (/^>/.test(block)) return `<blockquote>${inline(block.replace(/^>\s?/gm,''))}</blockquote>`; if (/^[-*]\s/.test(block)) return `<ul>${block.split('\n').map(x=>`<li>${inline(x.replace(/^[-*]\s/,''))}</li>`).join('')}</ul>`; return `<p>${inline(block.replace(/\n/g,'<br>'))}</p>`; }).join(''); };
const inline = s => s.replace(/`([^`]+)`/g,'<code>$1</code>').replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>').replace(/\[([^\]]+)\]\((https?:\/\/[^)]+)\)/g,'<a href="$2" target="_blank" rel="noreferrer">$1</a>');
const layout = (content, kicker, title, lede='') => `<div class="page"><div class="kicker">${kicker}</div><div class="section-head"><div><h1 class="article-title">${title}</h1>${lede?`<p class="lede">${lede}</p>`:''}</div></div>${content}</div>`;
const home = () => `<div class="page"><section class="hero"><div><div class="kicker">Static analysis · TypeScript + JavaScript</div><h1>Stop guessing.<br><em>Start proving.</em></h1><p class="lede">OptiPrune builds a dependency graph from your real entry points and shows what is never reached — files, imports, exports, packages, and logic.</p><div class="actions"><a class="button primary" href="/docs">Read the docs →</a><a class="button secondary" href="/plugins">Explore plugins</a></div><p class="blog-meta">npm install --save-dev @optiprune/cli · zero runtime dependencies · CI-ready</p></div><div class="hero-art"><img src="/assets/optiprune-animation.svg" alt="OptiPrune dependency graph animation" /></div></section><div class="metrics"><div class="metric"><strong>7</strong><span>analysis layers</span></div><div class="metric"><strong>3</strong><span>confidence levels</span></div><div class="metric"><strong>0</strong><span>runtime dependencies</span></div><div class="metric"><strong id="plugin-count">—</strong><span>ecosystem plugins</span></div></div><section class="section"><div class="section-head"><div><div class="kicker">Why OptiPrune</div><h2>Evidence over intuition.</h2></div><p>Designed for safe cleanup: inspect the graph, review confidence, then remove only what your codebase can prove is unreachable.</p></div><div class="feature-grid"><div class="card"><span class="number">01 / GRAPH</span><h3>Reachability, not heuristics</h3><p>Entry points create a real module graph instead of relying on naming guesses or arbitrary percentages.</p></div><div class="card"><span class="number">02 / CONTEXT</span><h3>Context-aware findings</h3><p>Dynamic dispatch, test conventions, generated files, and package metadata stay visible in the analysis.</p></div><div class="card"><span class="number">03 / CONTROL</span><h3>Dry-run first</h3><p>Every fix is explicit. Preview changes, use confidence gates, and keep a clean audit trail in CI.</p></div></div></section><section class="section"><div class="section-head"><div><div class="kicker">A small proof</div><h2>Scan. Verify. Prune.</h2></div></div><div class="terminal"><div>$ npx optiprune ./src</div><div class="warn">✖ [HIGH] src/legacy/orphan.ts is not reachable from any entry point</div><div class="warn">⚠ [MEDIUM] export 'legacyFn' is never imported</div><div class="ok">✓ 2 safe to remove · 1 needs review</div></div></section></div>`;
const docs = () => layout(`<div class="docs-layout"><aside class="toc"><a href="#install">01 Install</a><a href="#workflow">02 Workflow</a><a href="#config">03 Config</a><a href="#plugins">04 Plugins</a><a href="#guides">05 Guides</a></aside><article class="article"><section id="install"><h2>Install</h2><p>OptiPrune is a headless analyzer for TypeScript and JavaScript workspaces. Use the CLI for a quick scan or import Core when you need programmatic control.</p><pre>npm install --save-dev @optiprune/cli
npm install @optiprune/core</pre><p>Run from the workspace root:</p><pre>npx optiprune ./src --entry src/index.ts --dry-run</pre></section><section id="workflow"><h2>A predictable workflow</h2><p>Start with a dry run, inspect high-confidence findings, then widen the scope. OptiPrune keeps analysis and mutation separate so a CI check cannot silently rewrite your repository.</p><div class="feature-grid"><div class="card"><span class="number">1</span><h3>Discover</h3><p>Declare entry points and let the graph reveal reachable modules.</p></div><div class="card"><span class="number">2</span><h3>Review</h3><p>Use high, medium, and low confidence as a review queue.</p></div><div class="card"><span class="number">3</span><h3>Fix</h3><p>Apply only approved changes with a reproducible command.</p></div></div></section><section id="config"><h2>Configuration</h2><p>Keep configuration close to the project. Plugin overrides let you force-enable a detector or disable one that does not match your runtime conventions.</p><pre>{
  "entry": ["src/index.ts"],
  "plugins": { "nextjs-plugin": true, "nestjs-plugin": false },
  "failOn": "high"
}</pre></section><section id="plugins"><h2>Plugins</h2><p>Plugins add framework, test-runner, and tooling conventions to the graph. Browse the full inventory on the <a href="/plugins">Plugins page</a>, including source links and lifecycle hooks.</p></section><section id="guides"><h2>Guides and reference</h2><p>For CI, use JSON or SARIF reporters. For monorepos, define workspace boundaries explicitly. For custom integrations, import the headless Core API and keep file mutation behind your own approval step.</p></section></article></div>`, 'Documentation / Core + CLI', 'A clear path from first scan to safe cleanup.', 'No empty “go here” pages: the essentials, commands, configuration model, plugin behavior, and next steps live together.');
async function pluginsPage(){ const data=await fetch('/plugins.json').then(r=>r.json()).catch(()=>[]); return layout(`<div class="plugin-toolbar"><input id="plugin-search" placeholder="Filter plugins…" aria-label="Filter plugins" /><select id="plugin-category" aria-label="Filter by category"><option value="">All categories</option>${[...new Set(data.map(x=>x.category))].sort().map(x=>`<option>${esc(x)}</option>`).join('')}</select></div><p class="blog-meta">${data.length} plugins shipped from the current Core source tree.</p><div class="plugin-grid" id="plugin-grid">${data.map(pluginCard).join('')}</div>`, 'Ecosystem / Plugin registry', 'Plugins that understand your stack.', 'Frameworks, test runners, build tools, and project conventions — documented with source links instead of vague promises.'); }
const pluginCard = p => `<article class="card plugin-card" data-name="${esc(p.name)}" data-category="${esc(p.category)}"><span class="tag">${esc(p.category)}</span><span class="version">v${esc(p.version||'1.0.0')}</span><h3>${esc(p.name)}</h3><p>${esc(p.summary||'Framework-aware reachability support for this tool.')}</p><a href="${esc(p.source)}" target="_blank" rel="noreferrer">View source ↗</a></article>`;
async function blogPage(){ const posts=await fetch('/api/blog').then(r=>r.ok?r.json():[]).catch(()=>[]); return layout(posts.length?`<div class="blog-grid">${posts.map(p=>`<a class="card blog-card" href="/blog/${encodeURIComponent(p.id)}"><span class="kicker">${esc(p.category||'Engineering')}</span><h3>${esc(p.title)}</h3><p>${esc(p.excerpt||'Read the latest from OptiPrune.')}</p><div class="blog-meta">${new Date(p.published_at||p.created_at).toLocaleDateString()} · Read more →</div></a>`).join('')}</div>`:`<div class="empty">No public posts yet. Publish a KV record with <code>show_on_website: true</code> to make it appear here.</div>`, 'Journal / Updates', 'The OptiPrune blog.', 'Release notes, architecture notes, and practical cleanup patterns.'); }
async function blogDetail(id){ const p=await fetch('/api/blog/'+encodeURIComponent(id)).then(r=>r.ok?r.json():null).catch(()=>null); if(!p) return layout('<div class="empty">This post could not be found.</div>','Blog / 404','Post not found.'); return layout(`<article class="article blog-post"><div class="blog-meta">${new Date(p.published_at||p.created_at).toLocaleDateString()} · ${esc(p.category||'Engineering')}</div><div>${md(p.body||p.content||'')}</div></article>`,'Blog / '+esc(p.category||'Update'),esc(p.title),esc(p.excerpt||'')); }
async function show(){ const path=location.pathname.replace(/\/$/,'')||'/'; if(path==='/') app.innerHTML=home(); else if(path==='/docs') app.innerHTML=docs(); else if(path==='/plugins'){app.innerHTML=await pluginsPage(); bindPlugins();} else if(path==='/blog') app.innerHTML=await blogPage(); else if(path.startsWith('/blog/')) app.innerHTML=await blogDetail(decodeURIComponent(path.slice(6))); else app.innerHTML=layout('<div class="empty">The page you requested does not exist.</div>','404','Not found.'); fetch('/api/blog').then(r=>r.ok?r.json():[]).then(posts=>{const fresh=posts.find(p=>p.show_on_website && Date.now()-new Date(p.published_at||p.created_at).getTime()<172800000); const a=document.querySelector('#announcement'); if(fresh&&path!=='/blog/'+fresh.id){a.hidden=false;a.innerHTML=`↗ ${esc(fresh.title)} <a href="/blog/${encodeURIComponent(fresh.id)}">Learn more →</a>`;}}).catch(()=>{}); }
function bindPlugins(){const q=document.querySelector('#plugin-search'), c=document.querySelector('#plugin-category'); const filter=()=>document.querySelectorAll('.plugin-card').forEach(x=>x.style.display=(!q.value||x.dataset.name.includes(q.value.toLowerCase()))&&(!c.value||x.dataset.category===c.value)?'':'none'); q.oninput=filter;c.onchange=filter; const count=document.querySelector('#plugin-count'); if(count)count.textContent=document.querySelectorAll('.plugin-card').length;}
document.querySelector('[data-theme-toggle]').onclick=()=>{document.documentElement.dataset.theme=document.documentElement.dataset.theme==='mono'?'':'mono';}; fetch('/plugins.json').then(r=>r.json()).then(items=>{const count=document.querySelector('#plugin-count');if(count)count.textContent=items.length;}).catch(()=>{}); show();
